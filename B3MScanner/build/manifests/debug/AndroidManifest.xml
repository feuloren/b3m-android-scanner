<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:versionCode="1" android:versionName="1.0" package="b3m.application.scanner">

    <uses-sdk android:minSdkVersion="9" android:targetSdkVersion="14"/>

    <application android:allowBackup="true" android:debuggable="true" android:icon="@drawable/ic_launcher" android:label="@string/app_name" android:theme="@style/AppTheme">
        <activity android:label="@string/app_name" android:name="b3m.application.scanner.LoginActivity" android:windowSoftInputMode="adjustResize|stateVisible">

            <intent-filter>

                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>

            </intent-filter>
        </activity>
        <activity android:label="@string/app_name" android:name="b3m.application.scanner.ParametresActivity" android:parentActivityName="b3m.application.scanner.LoginActivity" android:windowSoftInputMode="adjustResize|stateVisible">
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data android:name="android.support.PARENT_ACTIVITY" android:value="b3m.application.scanner.LoginActivity"/>

        </activity>
        <activity android:label="@string/app_name" android:name="b3m.application.scanner.ScanActivity" android:parentActivityName="b3m.application.scanner.LoginActivity" android:windowSoftInputMode="adjustResize|stateVisible">
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data android:name="android.support.PARENT_ACTIVITY" android:value="b3m.application.scanner.LoginActivity"/>

        </activity>
    </application>

</manifest>
